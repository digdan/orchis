job_id: video_info
description: Gather information about a video file, then return info as outputs

inputs:
  video_path:
    type: string

outputs:
  duration_seconds:
    type: number
  resolution_width:
    type: number
  resolution_height:
    type: number